import { KilometerPacePipe } from './kilometer-pace.pipe';

describe('KilometerPacePipe', () => {
  it('create an instance', () => {
    const pipe = new KilometerPacePipe();
    expect(pipe).toBeTruthy();
  });
});
